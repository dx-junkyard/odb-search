version: '3.9'

services:
  fastapi-app:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - ./static:/static
    environment:
      - PYTHONUNBUFFERED=1
      - CATALOG_DIR=/static/catalog_json


